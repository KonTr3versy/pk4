# =============================================================================
# PurpleKit Environment Variables
# =============================================================================
# Copy this file to .env and update the values
# =============================================================================

# Database connection string
# Local development (using docker-compose):
DATABASE_URL=postgresql://purplekit:purplekit_dev_password@db:5432/purplekit

# Production (AWS RDS) - replace with your actual values:
# DATABASE_URL=postgresql://purplekit:YOUR_PASSWORD@your-rds-endpoint.region.rds.amazonaws.com:5432/purplekit

# Server configuration
NODE_ENV=development
PORT=3000

# JWT Secret for authentication (change this in production!)
JWT_SECRET=change-this-to-a-random-string-in-production
